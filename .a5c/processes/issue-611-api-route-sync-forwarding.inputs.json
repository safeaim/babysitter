{
  "issueNumber": 611,
  "title": "Fix remaining API route issues and sync parameter forwarding",
  "issueUrl": "https://github.com/a5c-ai/babysitter/issues/611",
  "labels": [
    "bug",
    "effort:medium",
    "ready-for-dev",
    "priority:medium",
    "krate",
    "krate-web",
    "risk:medium"
  ],
  "issueBody": "## Summary\n\nAudit of all 55 API routes found several remaining issues. Two critical signature mismatches were already fixed (conflict resolve + write intent cancel), but these remain:\n\n## Remaining Issues\n\n### 1. External sync route ignores body parameters\n\n**File:** `app/api/orgs/[org]/external/sync/route.js`\n\nThe route passes only `body.bindingName` to `controller.syncExternalBinding()`, but the MCP tool `krate_sync_external` and the controller accept additional parameters: `kind`, `localName`, `spec`, `externalEnvelope`, `watermark`.\n\n**Fix:** Forward all body parameters to the controller.\n\n### 2. Potential method name mismatches (verify)\n\n- `app/api/orgs/[org]/inference/services/[name]/infer/route.js` calls `controller.listResources()` - verify this exists (SDK exports `listResource` singular)\n- `app/api/orgs/[org]/assistant/generate/route.js` calls `listResourceForOrg()` - verify this exists\n\n### 3. Curated model catalog deploy error is silent\n\n**File:** `app/components/inference/curated-model-catalog.jsx` line 124\n\nThe \"Deploy\" action on a curated model has a `.catch((err) => console.warn(...))` - deploy is user-initiated and should show an error state.\n\n## Already Fixed (for reference)\n\n| Route | Issue | Status |\n|-------|-------|--------|\n| `/external/conflicts/[id]/resolve` | Positional args -> named object | Fixed |\n| `/external/write-intents/[name]/cancel` | Positional arg -> named object with cancelledBy | Fixed |\n\n## Reference\n\n- [docs/gaps/api-route-issues.md](packages/krate/docs/gaps/api-route-issues.md)\n\n## Dependencies\n\nNone",
  "triageComment": "## Triage Summary\n\nIssue #611 is a valid Krate web/API bug bundle, but one listed method-name concern is a false positive.\n\n## Critical Evaluation\n\n- The external sync route issue is valid: `packages/krate/web/app/api/orgs/[org]/external/sync/route.js` passes only `body.bindingName` to `controller.syncExternalBinding()`, while the core controller and MCP tool support an options object with `kind`, `localName`, `namespace`, `spec`, `externalEnvelope`, and `watermark`.\n- The inference infer route issue is valid: `packages/krate/web/app/api/orgs/[org]/inference/services/[name]/infer/route.js` calls `controller.listResources('KrateVirtualModel')`, but `createKrateApiController()` exposes `listResource()` and `listResourceForOrg()`, not `listResources()`.\n- The assistant generate concern is not valid as written: `controller.listResourceForOrg(org, 'KrateVirtualModel')` exists in `packages/krate/core/src/api-controller.js` and is covered by org-scoping tests.\n- The curated catalog deploy issue is valid, but the precise problem is partial-success reporting: the service deploy error is surfaced, while the follow-up route auto-create failure is only logged with `console.warn()` and the UI still reports success.\n\n## Recommended Approach\n\n1. In `external/sync/route.js`, keep `bindingName` as the first argument and pass the remaining request body fields as the second options object. Preserve binding-name-only callers.\n2. In `inference/services/[name]/infer/route.js`, replace `controller.listResources('KrateVirtualModel')` with `controller.listResourceForOrg(org, 'KrateVirtualModel')` if hooks should be org-scoped, or `controller.listResource('KrateVirtualModel')` if global matching is intended.\n3. Do not change `assistant/generate/route.js` for the `listResourceForOrg()` concern.\n4. In `curated-model-catalog.jsx`, surface route auto-create failure as either a partial-success warning or make deploy fail if the route is mandatory. Product semantics should decide which.\n5. Add focused web route/component tests so these controller contract mismatches are caught at the web boundary, not just in core controller tests.",
  "targetFiles": [
    "packages/krate/web/app/api/orgs/[org]/external/sync/route.js",
    "packages/krate/web/app/api/orgs/[org]/inference/services/[name]/infer/route.js",
    "packages/krate/web/app/api/orgs/[org]/assistant/generate/route.js",
    "packages/krate/web/app/components/inference/curated-model-catalog.jsx",
    "packages/krate/core/src/api-controller.js",
    "packages/krate/cli/src/mcp-server.js",
    "packages/krate/docs/gaps/api-route-issues.md",
    "packages/krate/docs/gaps/ui-ux-remaining.md",
    "packages/krate/web/tests"
  ],
  "expectedRuntimeCallPaths": [
    "POST /api/orgs/{org}/external/sync -> packages/krate/web/app/api/orgs/[org]/external/sync/route.js -> createKrateApiController({ namespace: orgNamespaceName(org) }) -> controller.syncExternalBinding(bindingName, options)",
    "POST /api/orgs/{org}/inference/services/{name}/infer -> packages/krate/web/app/api/orgs/[org]/inference/services/[name]/infer/route.js -> controller.getResource('KrateInferenceService', name) -> controller.listResourceForOrg(org, 'KrateVirtualModel') -> createVirtualModelHookBridge",
    "CuratedModelCatalog deploy button -> packages/krate/web/app/components/inference/curated-model-catalog.jsx -> POST /api/orgs/{org}/inference/services -> POST /api/orgs/{org}/inference/routes -> visible success/partial failure state"
  ],
  "verificationCommands": [
    "npm run test --workspace=@a5c-ai/krate-web",
    "npm run build:krate-web"
  ],
  "maxRefinements": 2
}
