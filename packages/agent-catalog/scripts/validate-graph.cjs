// Graph validation is now handled by the Atlas indexer at build time.
// This script is a no-op retained for backward compatibility.
console.log("[validate-graph] Skipped — graph data lives in @a5c-ai/atlas.");
