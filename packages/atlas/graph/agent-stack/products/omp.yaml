nodeKind: AgentProduct
id: agent:omp
attributes:
  displayName: "OMP (oh-my-pi)"
  vendor: "oh-my-pi"
  homepageUrl: "https://www.npmjs.com/package/@oh-my-pi/pi-coding-agent"
  primarySurface: cli
  supportTier: adapter-only
  productKind: full-cli-agent
  stackScope: full
  description: |
    Community fork of Pi (oh-my-pi) — a terminal coding agent surfaced
    through the v6 catalog via the agent-mux adapter. Inherits Pi's
    plugin shape but maintained independently.
edges:
  has_version:
    - agentVersion:omp:ge-0-0-0
