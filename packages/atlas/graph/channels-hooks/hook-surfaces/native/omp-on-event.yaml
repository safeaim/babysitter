nodeKind: HookSurface
id: hook-surface:omp.on-event
attributes:
  eventName: "onEvent (OMP)"
  displayName: "onEvent (OMP)"
  kind: native
  originVendor: a5c
  direction: post
  blocking: false
  family: shell-hook
  payloadSchema:
    type: object
    required: [event]
    properties:
      event: { type: string }
      payload: { type: object }
  description: |
    OMP's generic onEvent hook. Canonicalizes to post-tool-use for
    cross-product policies.
edges:
  canonicalized_to:
    - target: hook-surface:post-tool-use
