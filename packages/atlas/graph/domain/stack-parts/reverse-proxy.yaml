nodeKind: StackPart
id: stack-part:reverse-proxy
attributes:
  displayName: "Reverse Proxy"
  category: infrastructure
  description: |
    HTTP-layer proxy in front of one or more upstream servers. Examples: nginx, Caddy, HAProxy.
edges:
  stack_part_used_by:
    - target: skill-area:service-mesh-config
