nodeKind: Tool
id: tool:gitlab-ci
attributes:
  displayName: "GitLab CI/CD"
  homepageUrl: "https://docs.gitlab.com/ee/ci/"
  kind: ci
  description: |
    CI/CD service built into GitLab - pipelines defined in .gitlab-ci.yml.
edges:
  belongs_to_language:
    - target: language:yaml
  used_for:
    - target: skill-area:ci-cd
  alternative_to:
    - target: tool:github-actions
      comparison: "CI/CD platforms — GitLab CI is GitLab-native; GitHub Actions is GitHub-native"
    - target: tool:circleci
      comparison: "CI/CD platforms"
    - target: tool:buildkite
      comparison: "CI/CD — GitLab CI is integrated; Buildkite is hybrid"
    - target: tool:jenkins
      comparison: "CI/CD — GitLab CI is managed; Jenkins is self-hosted"
