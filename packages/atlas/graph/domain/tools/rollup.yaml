nodeKind: Tool
id: tool:rollup
attributes:
  displayName: "Rollup"
  homepageUrl: "https://rollupjs.org"
  kind: bundler
  description: |
    Module bundler for JavaScript that compiles small pieces of code into something larger.
edges:
  belongs_to_language:
    - target: language:javascript
  used_for:
    - target: skill-area:ci-pipeline-optimization
  alternative_to:
    - target: tool:esbuild
      comparison: "JavaScript bundlers — Rollup is plugin-rich; esbuild is Go-based and extremely fast"
    - target: tool:babel
      comparison: "JavaScript build tools — Rollup is a bundler; Babel is a transpiler"
