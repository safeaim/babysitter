---
# IaC / Platform Engineering tools
nodeKind: Tool
id: tool:terraform-cloud
attributes:
  displayName: "Terraform Cloud"
  homepageUrl: "https://app.terraform.io"
  kind: iac
  languageApplicability: neutral
  description: |
    HashiCorp's managed service for Terraform — provides remote state storage,
    collaborative plan/apply runs, policy enforcement via Sentinel, and an
    API-driven workspace model for team-based infrastructure workflows.
edges:
  tool_used_by:
    - target: skill-area:terraform-infrastructure
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  alternative_to:
    - target: tool:pulumi-cloud
      comparison: "Managed IaC platforms — Terraform Cloud uses HCL; Pulumi Cloud uses programming languages"
    - target: tool:ansible
      comparison: "IaC/config — Terraform Cloud is declarative provisioning; Ansible is agentless config management"
---
nodeKind: Tool
id: tool:pulumi-cloud
attributes:
  displayName: "Pulumi Cloud"
  homepageUrl: "https://www.pulumi.com/product/pulumi-cloud/"
  kind: iac
  languageApplicability: neutral
  description: |
    Pulumi's SaaS control plane for infrastructure-as-code — hosts stack state,
    provides secrets management, policy enforcement, drift detection, and
    collaborative deployment history across cloud providers.
edges:
  tool_used_by:
    - target: skill-area:terraform-infrastructure
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  alternative_to:
    - target: tool:terraform-cloud
      comparison: "Managed IaC platforms — Pulumi Cloud uses programming languages; Terraform Cloud uses HCL"
    - target: tool:ansible
      comparison: "IaC/config — Pulumi Cloud is code-based provisioning; Ansible is imperative playbooks"
---
nodeKind: Tool
id: tool:crossplane
attributes:
  displayName: "Crossplane"
  homepageUrl: "https://www.crossplane.io"
  kind: iac
  description: |
    CNCF project that extends Kubernetes with Composite Resource Definitions and
    Managed Resources, enabling platform teams to build internal cloud APIs and
    provision external infrastructure declaratively from the Kubernetes control plane.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:gitops
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  alternative_to:
    - target: tool:terraform
      comparison: "IaC — Crossplane uses Kubernetes CRDs; Terraform uses HCL files"
    - target: tool:pulumi
      comparison: "IaC — Crossplane uses Kubernetes CRDs; Pulumi uses programming languages"
    - target: tool:opentofu
      comparison: "IaC — Crossplane uses Kubernetes CRDs; OpenTofu uses HCL files"
---
nodeKind: Tool
id: tool:backstage
attributes:
  displayName: "Backstage"
  homepageUrl: "https://backstage.io"
  kind: orchestrator
  description: |
    CNCF open-source developer portal platform by Spotify — provides a software
    catalog, TechDocs, scaffolder templates, and plugin ecosystem for building
    unified internal developer portals that improve developer experience.
edges:
  belongs_to_language:
    - language:typescript
  tool_used_by:
    - target: skill-area:platform-engineering
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:developer-environment
  alternative_to:
    - target: tool:port-dev
      comparison: "Internal developer portals — Backstage is open-source plugin-based; Port is managed SaaS"
    - target: tool:devtron
      comparison: "Platform engineering — Backstage is a developer portal; Devtron is K8s app management"
    - target: tool:kubevela
      comparison: "Platform engineering — Backstage is a developer portal; KubeVela is OAM-based delivery"
    - target: tool:rancher
      comparison: "Platform engineering — Backstage is a developer portal; Rancher is K8s cluster management"
---
nodeKind: Tool
id: tool:port-dev
attributes:
  displayName: "Port"
  homepageUrl: "https://www.getport.io"
  kind: orchestrator
  languageApplicability: neutral
  description: |
    Developer portal and internal developer platform (IDP) — provides a software
    catalog, self-service actions, scorecards, and workflow automation to give
    developers full visibility and control over their engineering stack.
edges:
  tool_used_by:
    - target: skill-area:platform-engineering
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:developer-environment
  alternative_to:
    - target: tool:backstage
      comparison: "Internal developer portals — Port is managed SaaS; Backstage is open-source plugin-based"
    - target: tool:devtron
      comparison: "Platform engineering tools"
    - target: tool:kubevela
      comparison: "Platform engineering tools"
---
# GitOps tools
nodeKind: Tool
id: tool:helmfile
attributes:
  displayName: "Helmfile"
  homepageUrl: "https://github.com/helmfile/helmfile"
  kind: orchestrator
  description: |
    Declarative spec for deploying Helm charts across environments — supports
    templating, layered values, selective sync, and diff previews, enabling
    GitOps-style management of multi-chart Kubernetes deployments.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:gitops
  used_for:
    - target: skill-area:gitops
    - target: skill-area:deployment-automation
  alternative_to:
    - target: tool:helm
      comparison: "Kubernetes deployment — Helmfile orchestrates multi-chart deployments; Helm manages single charts"
    - target: tool:kustomize
      comparison: "Kubernetes config management"
---
# Container / local cluster tools
nodeKind: Tool
id: tool:docker-compose
attributes:
  displayName: "Docker Compose"
  homepageUrl: "https://docs.docker.com/compose/"
  kind: container
  description: |
    YAML-based tool for defining and running multi-container Docker applications
    locally or in CI — manages service dependencies, networking, volumes, and
    environment configuration in a single compose file.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:containerization
    - target: skill-area:local-development
  alternative_to:
    - target: tool:k3s
      comparison: "Local container orchestration — Docker Compose is simple multi-container; K3s is full Kubernetes"
    - target: tool:kind
      comparison: "Local container orchestration — Docker Compose is multi-container; kind is Kubernetes-in-Docker"
    - target: tool:minikube
      comparison: "Local container orchestration — Docker Compose is multi-container; minikube is local Kubernetes"
    - target: tool:colima
      comparison: "Local container tools — Docker Compose orchestrates containers; Colima provides the container runtime"
---
nodeKind: Tool
id: tool:k3s
attributes:
  displayName: "K3s"
  homepageUrl: "https://k3s.io"
  kind: container
  description: |
    CNCF-certified lightweight Kubernetes distribution by Rancher/SUSE — designed
    for edge, IoT, and resource-constrained environments; ships as a single binary
    with embedded SQLite or etcd and minimal memory footprint.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  implements_stack_part:
    - target: stack-part:kubernetes-cluster
  alternative_to:
    - target: tool:kind
      comparison: "Local Kubernetes — K3s is a lightweight distro; kind runs clusters in Docker containers"
    - target: tool:minikube
      comparison: "Local Kubernetes — K3s is lightweight and edge-ready; minikube is single-node with add-ons"
    - target: tool:docker-compose
      comparison: "Local container orchestration — K3s is full Kubernetes; Docker Compose is simple multi-container"
---
nodeKind: Tool
id: tool:kind
attributes:
  displayName: "kind"
  homepageUrl: "https://kind.sigs.k8s.io"
  kind: container
  description: |
    Kubernetes IN Docker — runs multi-node Kubernetes clusters inside Docker
    containers, primarily used for local development and CI testing of Kubernetes
    controllers, operators, and admission webhooks.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:local-development
    - target: skill-area:containerization
  alternative_to:
    - target: tool:minikube
      comparison: "Local Kubernetes — kind uses Docker containers; minikube uses VMs or containers"
    - target: tool:k3s
      comparison: "Local Kubernetes — kind runs clusters in Docker containers; K3s is a lightweight distro"
    - target: tool:docker-compose
      comparison: "Local container orchestration — kind is Kubernetes-in-Docker; Docker Compose is multi-container"
---
nodeKind: Tool
id: tool:minikube
attributes:
  displayName: "minikube"
  homepageUrl: "https://minikube.sigs.k8s.io"
  kind: container
  description: |
    Local Kubernetes cluster tool that provisions a single-node cluster using VM
    or container drivers — offers add-ons for ingress, dashboard, metrics-server,
    and GPU support, ideal for developer iteration and local testing.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:local-development
    - target: skill-area:containerization
  alternative_to:
    - target: tool:kind
      comparison: "Local Kubernetes — minikube uses VMs or containers; kind uses Docker containers"
    - target: tool:k3s
      comparison: "Local Kubernetes — minikube is single-node with add-ons; K3s is lightweight and edge-ready"
    - target: tool:docker-compose
      comparison: "Local container orchestration — minikube is local Kubernetes; Docker Compose is multi-container"
---
nodeKind: Tool
id: tool:rancher
attributes:
  displayName: "Rancher"
  homepageUrl: "https://www.rancher.com"
  kind: orchestrator
  description: |
    SUSE's open-source Kubernetes management platform — provides a multi-cluster
    management UI, role-based access control, App Catalog (Helm charts), Fleet
    GitOps engine, and integrations for RKE2 and K3s cluster provisioning.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  implements_stack_part:
    - target: stack-part:kubernetes-cluster
  alternative_to:
    - target: tool:backstage
      comparison: "Platform engineering — Rancher is K8s cluster management; Backstage is a developer portal"
    - target: tool:devtron
      comparison: "Kubernetes management platforms"
    - target: tool:kubevela
      comparison: "Kubernetes platforms — Rancher manages clusters; KubeVela manages app delivery"
---
nodeKind: Tool
id: tool:nomad
attributes:
  displayName: "Nomad"
  homepageUrl: "https://www.nomadproject.io"
  kind: orchestrator
  description: |
    HashiCorp's flexible workload orchestrator — schedules containerized, VM,
    and bare-metal jobs across cloud and on-prem; integrates natively with Consul
    for service mesh and Vault for secrets, supporting diverse deployment patterns.
edges:
  belongs_to_language:
    - language:go
  tool_used_by:
    - target: skill-area:containerization
  used_for:
    - target: skill-area:platform-engineering
    - target: skill-area:deployment-automation
  alternative_to:
    - target: tool:kubernetes
      comparison: "Container orchestration platforms"
