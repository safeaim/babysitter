nodeKind: DiscoverySignal
id: discovery-signal:copilot-cli-host-env
attributes:
  signalId: discovery-signal:copilot-cli-host-env
  signalKind: env-any
  key: copilot
  matchMode: any
  confidence: medium
  scope: host-detection
  signals:
    - COPILOT_CLI_SESSION
    - GH_COPILOT_SESSION
  argvMatches:
    - copilot
  consumer: agent-comm-mux
  metadataFields:
    - { key: session_id, envVars: [COPILOT_CLI_SESSION, GH_COPILOT_SESSION] }
edges:
  applies_to:
    - target: agent:copilot-cli
