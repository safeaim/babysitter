# error and runtime-hook scenarios from
# @a5c-ai/agent-mux-harness-mock. Promotes the `error:*` (5) and
# `runtimeHook*` (3) deterministic fixtures to InteractionPrimitive
# records (kind=mock-scenario) so test plans can be regenerated from
# the v6 graph alone.
# Source: packages/agent-mux/harness-mock/src/scenarios/{errors,hooks}.ts.
# Wired via @a5c-ai/agent-mux-harness-mock PackageSurface
# `exposes_subcommand` edge.
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-error-rate-limit
attributes:
  displayName: "harness-mock error:rate-limit (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Mock claude-code session that emits a Rate-limit error stdout chunk and
    HTTP 429 stderr chunk, exits with code 1. Expected mapped code:
    RATE_LIMITED.
    Source: packages/agent-mux/harness-mock/src/scenarios/errors.ts:25-36
    (rateLimitClaude; ERROR_SCENARIOS['rate-limit']).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-error-auth-required
attributes:
  displayName: "harness-mock error:auth-required (codex)"
  kind: mock-scenario
  surface: cli
  description: |
    Mock codex session that emits "Missing OPENAI_API_KEY" + "auth:
    unauthorized" and exits with code 1. Expected mapped code: AUTH_ERROR.
    Source: packages/agent-mux/harness-mock/src/scenarios/errors.ts:38-49
    (authRequiredCodex; ERROR_SCENARIOS['auth-required']).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-error-crash
attributes:
  displayName: "harness-mock error:crash (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Mock claude-code session that crashes after 80ms with SIGTERM, emits
    'segfault' on stderr; exitCode 1. Expected mapped code: AGENT_CRASH.
    Source: packages/agent-mux/harness-mock/src/scenarios/errors.ts:51-61
    (crashClaude; ERROR_SCENARIOS.crash).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-error-timeout
attributes:
  displayName: "harness-mock error:timeout (gemini)"
  kind: mock-scenario
  surface: cli
  description: |
    Mock gemini session that hangs (process.hang=true) emitting only
    "Working..." Expected mapped code: TIMEOUT (driven by onTimeout, not
    process exit).
    Source: packages/agent-mux/harness-mock/src/scenarios/errors.ts:63-73
    (timeoutGemini; ERROR_SCENARIOS.timeout).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-error-oom
attributes:
  displayName: "harness-mock error:oom (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Mock claude-code session killed with SIGKILL after 60ms, exitCode 137,
    stderr "FATAL ERROR: Reached heap limit". Expected mapped code:
    AGENT_CRASH.
    Source: packages/agent-mux/harness-mock/src/scenarios/errors.ts:75-85
    (oomClaude; ERROR_SCENARIOS.oom).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-runtime-hook-allow-bash
attributes:
  displayName: "harness-mock runtimeHookAllowBash (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Drives the claude-code preToolUse runtime hook with decision='allow' on
    a Bash tool call ('echo ok'); session emits init / tool_call /
    tool_result / result(success) wire chunks; exitCode 0.
    Source: packages/agent-mux/harness-mock/src/scenarios/hooks.ts:72-93
    (RUNTIME_HOOK_SCENARIOS.runtimeHookAllowBash).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-runtime-hook-deny-write
attributes:
  displayName: "harness-mock runtimeHookDenyWrite (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Drives the claude-code preToolUse runtime hook with decision='deny' on
    a Write tool call (path='blocked.txt'); errorMessage='Runtime hook
    denied Write'.
    Source: packages/agent-mux/harness-mock/src/scenarios/hooks.ts:95-116
    (RUNTIME_HOOK_SCENARIOS.runtimeHookDenyWrite).
---
nodeKind: InteractionPrimitive
id: interaction-primitive:agent-mux-harness-mock-runtime-hook-timeout
attributes:
  displayName: "harness-mock runtimeHookTimeout (claude-code)"
  kind: mock-scenario
  surface: cli
  description: |
    Drives the claude-code preToolUse runtime hook with decision='timeout'
    on a Bash 'sleep 1' tool call; process.hang=true so the hook timeout
    branch fires.
    Source: packages/agent-mux/harness-mock/src/scenarios/hooks.ts:118-136
    (RUNTIME_HOOK_SCENARIOS.runtimeHookTimeout).
