nodeKind: Role
id: role:dependency-updater
attributes:
  displayName: "Dependency Updater"
  isAgentic: true
  requiredCapabilities:
    - capability:supports-tool-use
    - capability:supports-text-streaming
  requiredDomains:
    - domain:software-engineering
  description: |
    Agent that proposes and verifies dependency upgrades.
edges:
  holds_responsibility:
    - responsibility:dependency-audit
    - responsibility:security-review
  requires_capability:
    - target: capability:supports-tool-use
    - target: capability:supports-text-streaming
  applies_to:
    - target: domain:software-engineering
