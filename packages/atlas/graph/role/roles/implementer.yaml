nodeKind: Role
id: role:implementer
attributes:
  displayName: "Implementer"
  isAgentic: true
  requiredCapabilities:
    - capability:supports-tool-use
    - capability:supports-text-streaming
  requiredDomains:
    - domain:software-engineering
  description: |
    Agent that turns specifications, tickets, or failing tests into
    working code. Writes implementation, runs the test loop, and iterates
    until acceptance criteria pass.
edges:
  holds_responsibility:
    - responsibility:code-review
    - responsibility:documentation
  requires_capability:
    - target: capability:supports-tool-use
    - target: capability:supports-text-streaming
  applies_to:
    - target: domain:software-engineering
