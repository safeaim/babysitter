nodeKind: Role
id: role:test-writer
attributes:
  displayName: "Test Writer"
  isAgentic: true
  requiredCapabilities:
    - capability:supports-tool-use
    - capability:supports-text-streaming
  requiredDomains:
    - domain:software-engineering
  description: |
    Agent that authors unit, integration, and regression tests for existing code.
edges:
  holds_responsibility:
    - responsibility:test-strategy
    - responsibility:code-review
  requires_capability:
    - target: capability:supports-tool-use
    - target: capability:supports-text-streaming
  applies_to:
    - target: domain:software-engineering
