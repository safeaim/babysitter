nodeKind: Role
id: role:typo-fixer
attributes:
  displayName: "Typo Fixer"
  isAgentic: true
  requiredCapabilities:
    - capability:supports-tool-use
    - capability:supports-text-streaming
  requiredDomains:
    - domain:software-engineering
  description: |
    Agent that finds and corrects typos in code and documentation.
edges:
  holds_responsibility:
    - responsibility:documentation
    - responsibility:code-review
  requires_capability:
    - target: capability:supports-tool-use
    - target: capability:supports-text-streaming
  applies_to:
    - target: domain:software-engineering
