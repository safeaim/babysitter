nodeKind: ScopeBoundary
id: scope-boundary:tool-server-mcp-aws-s3.scope
attributes:
  subjectId: tool-server:mcp-aws-s3
  inScope: |
    AWS S3 bucket and object operations — list, get, put, delete, presigned URLs.
  outOfScope: |
    Other AWS services (use service-specific MCPs), cross-account IAM management, and large-scale data-pipeline orchestration.
  outOfScopeReasonIds:
    - out-of-scope-reason:future-phase
    - out-of-scope-reason:implementation-detail
edges:
  bounds_subject:
    - tool-server:mcp-aws-s3
