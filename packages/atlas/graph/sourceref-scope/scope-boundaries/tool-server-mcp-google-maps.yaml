nodeKind: ScopeBoundary
id: scope-boundary:tool-server-mcp-google-maps.scope
attributes:
  subjectId: tool-server:mcp-google-maps
  inScope: |
    Google Maps lookups — geocoding, places, directions, distance-matrix.
  outOfScope: |
    Real-time navigation rendering, offline mapping, and arbitrary Google Cloud Platform admin operations.
  outOfScopeReasonIds:
    - out-of-scope-reason:future-phase
    - out-of-scope-reason:implementation-detail
edges:
  bounds_subject:
    - tool-server:mcp-google-maps
