nodeKind: ScopeBoundary
id: scope-boundary:tool-server-mcp-linear.scope
attributes:
  subjectId: tool-server:mcp-linear
  inScope: |
    Linear issue-tracker access — issues, projects, comments, cycles, search.
  outOfScope: |
    Jira / GitHub Issues / ClickUp (use service-specific MCPs) and Linear admin / billing operations.
  outOfScopeReasonIds:
    - out-of-scope-reason:future-phase
    - out-of-scope-reason:implementation-detail
edges:
  bounds_subject:
    - tool-server:mcp-linear
