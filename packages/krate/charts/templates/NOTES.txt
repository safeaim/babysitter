Krate MVP package installed as {{ include "krate.fullname" . }} in namespace {{ .Release.Namespace }}.

Next local checks:
  kubectl get crd repositories.krate.a5c.ai # or enable apiService.enabled for aggregated APIService mode
  kubectl get repositories.krate.a5c.ai -n {{ .Release.Namespace }}
  kubectl get deploy,statefulset,svc -n {{ .Release.Namespace }}

For deterministic local validation without a cluster, run:
  npm run e2e
  npm run package:check
