/**
 * useNavigation — convenience re-export of the navigation hook.
 *
 * Follows the same pattern as useSession.ts, useTheme.ts, useClock.ts.
 */

export { useNavigation } from "../contexts/NavigationContext.js";
